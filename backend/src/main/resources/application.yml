server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mesdb?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: mes
    password: Mes1234!
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug

jwt:
  secret: mes-mini-project-jwt-secret-key-2026-very-secure
  expiration-ms: 86400000
